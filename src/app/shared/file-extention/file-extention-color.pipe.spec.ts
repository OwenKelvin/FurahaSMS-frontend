import { FileExtentionColorPipe } from './file-extention-color.pipe';

describe('FileExtentionColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtentionColorPipe();
    expect(pipe).toBeTruthy();
  });
});
