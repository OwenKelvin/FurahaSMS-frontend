import { TimetableEditorDirective } from './timetable-editor.directive';

describe('TimetableEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimetableEditorDirective();
    expect(directive).toBeTruthy();
  });
});
