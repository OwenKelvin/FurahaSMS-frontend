import { FileExtentionIconPipe } from './file-extention-icon.pipe';

describe('FileExtentionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtentionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
